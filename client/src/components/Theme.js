// theme.js
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#070d1e', // Set your desired primary color
    },
    // secondary: {
    //   main: '#FF5722', // Set your desired secondary color
    // },
    // You can define other palette colors here (e.g., success, warning, error)
  },
});

export default theme;
